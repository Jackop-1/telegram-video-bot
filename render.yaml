services:
  - type: worker
    name: telegram-video-bot
    env: docker
    plan: free
    autoDeploy: true
    buildCommand: ""
    startCommand: "python bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: OWNER_ID
        sync: false
